stages:
- stage: Build
  jobs:
  - job: Build
    pool:
      vmImage: 'Ubuntu-16.04'
    continueOnError: true
    steps:
    - script: echo "hello to my first Build"    

    - task: Docker@2
      displayName: Login to Aqua
      inputs:
        command: login
        containerRegistry: jakeaquaregistry

    - task: aquasecScanner@4
      inputs:
        image: 'nginx:latest'
        scanType: 'local'
        register: false
        hideBase: false
        showNegligible: false
        windowsScannerVersion: '4.2.0'
        scanner: 'registry.aquasec.com/scanner:6.2'
        runOptions: 'docker pull nginx:latest'
        registry: 'docker.io'
        connection: 'Aqua Demo Console Jake'
        caCertificates: true